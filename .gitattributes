# Auto detect text files and perform LF normalization
* text=auto

# Ensure shell scripts always use LF
*.sh text eol=lf
*.bash text eol=lf

# Windows scripts
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
